{
  "mcpServers": {
    "ninja-coder": {
      "command": "ninja-coder",
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    },
    "ninja-researcher": {
      "command": "ninja-researcher",
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    },
    "ninja-secretary": {
      "command": "ninja-secretary",
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    }
  }
}
