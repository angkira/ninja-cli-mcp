# 🔄 NINJA-MCP UPDATE INSTRUCTIONS

## ❌ ПОЧЕМУ НЕ РАБОТАЛО

Проблема была в том, что:

1. **`git pull`** - обновляет только ИСХОДНЫЙ КОД в репозитории
2. **Установленные бинарники** находятся в `~/.local/share/uv/tools/ninja-mcp/`
3. **Они НЕ обновляются автоматически** после `git pull`

**Ключевой момент:** Команда `uv tool install --reinstall --force .` должна запускаться **ИЗ ДИРЕКТОРИИ РЕПОЗИТОРИЯ**, потому что `.` означает "текущая директория"!

## ✅ ПРАВИЛЬНЫЙ СПОСОБ ОБНОВЛЕНИЯ

### Вариант 1: Автоматический (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Перейди в директорию репозитория
cd /path/to/ninja-cli-mcp

# 2. Запусти скрипт полного обновления
./FORCE_UPDATE.sh
```

Этот скрипт делает ВСЕ:
- ✓ Backup credentials
- ✓ Git pull
- ✓ Удаление старой установки
- ✓ Чистка кеша
- ✓ Установка из текущей директории
- ✓ Миграция credentials
- ✓ Перезапуск daemons
- ✓ Проверка что все работает

### Вариант 2: Ручной

```bash
# 1. Перейди в директорию репозитория
cd /path/to/ninja-cli-mcp

# 2. Backup credentials
cp ~/.ninja/credentials.db ~/.ninja/credentials.db.backup-$(date +%s)

# 3. Pull код
git pull

# 4. ПОЛНОСТЬЮ удали старую установку
uv tool uninstall ninja-mcp

# 5. Установи ЗАНОВО из ТЕКУЩЕЙ директории
uv tool install --force .

# 6. Перезапусти daemons
ninja-daemon restart
```

## 🔍 ДИАГНОСТИКА

Если после обновления все еще видишь старый интерфейс:

```bash
cd /path/to/ninja-cli-mcp
./diagnose_installation.sh
```

Этот скрипт покажет:
- Где установлены бинарники
- Какая версия кода в установленном пакете
- Что именно не обновилось
- Как это исправить

## 🗂️ ГДЕ ЧТО НАХОДИТСЯ

### Установленный пакет
```
~/.local/share/uv/tools/ninja-mcp/
├── bin/                          # Бинарники
│   ├── ninja-config
│   ├── ninja-daemon
│   └── ...
└── lib/python3.12/site-packages/ # Python модули
    ├── ninja_config/
    │   ├── ui/                   # НОВЫЙ модульный UI
    │   │   ├── main_menu.py
    │   │   ├── component_setup.py
    │   │   └── ...
    │   └── interactive_configurator.py
    ├── ninja_coder/
    └── ...
```

### Бинарники (symlinks)
```
~/.local/bin/ninja-config -> ~/.local/share/uv/tools/ninja-mcp/bin/ninja-config
```

### Credentials и config
```
~/.ninja/
├── credentials.db        # Зашифрованные credentials (AES-256-GCM)
└── config.json          # Новый формат конфига

~/.ninja-mcp.env         # Старый формат (должен быть мигрирован)
```

## 📋 CHECKLIST ПОСЛЕ ОБНОВЛЕНИЯ

После запуска `./FORCE_UPDATE.sh` проверь:

1. ✅ Запусти `ninja-config configure`
2. ✅ Должен увидеть "NINJA MCP POWER CONFIGURATOR" со звездами
3. ✅ В меню должны быть опции:
   - 📋 Configuration Overview
   - 🎯 Coder Setup
   - 🔑 API Key Management
   - 🤖 Model Selection
   - И т.д.

## 🚨 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

1. **Запусти диагностику:**
   ```bash
   cd /path/to/ninja-cli-mcp
   ./diagnose_installation.sh
   ```

2. **Отправь мне вывод диагностики** - я точно скажу что не так

3. **Ядерный вариант** (если ничего не помогает):
   ```bash
   # Полностью удали все
   uv tool uninstall ninja-mcp
   rm -rf ~/.local/share/uv/tools/ninja-mcp
   rm -rf ~/.cache/ninja-mcp

   # Backup credentials
   cp ~/.ninja/credentials.db ~/credentials.db.backup

   # Переустанови с нуля
   cd /path/to/ninja-cli-mcp
   uv tool install .

   # Восстанови credentials
   cp ~/credentials.db.backup ~/.ninja/credentials.db

   # Перезапусти
   ninja-daemon restart
   ```

## 🎯 КЛЮЧЕВЫЕ МОМЕНТЫ

1. **ВСЕГДА запускай update из директории репозитория**
   ```bash
   cd /path/to/ninja-cli-mcp  # ← ЭТО КРИТИЧЕСКИ ВАЖНО!
   ./FORCE_UPDATE.sh
   ```

2. **`git pull` НЕ обновляет установленный пакет** - только исходный код

3. **`uv tool install --force .`** - устанавливает из ТЕКУЩЕЙ директории (`.` = текущая директория)

4. **Credentials сохраняются** - они в `~/.ninja/credentials.db`, не трогаются при обновлении

5. **После обновления всегда перезапускай daemons:**
   ```bash
   ninja-daemon restart
   ```

---

Если что-то непонятно или не работает - запускай `./diagnose_installation.sh` и отправляй мне вывод!
