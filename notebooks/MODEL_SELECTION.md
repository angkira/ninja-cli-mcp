# 🚀 Упрощённая система выбора моделей

## Что сделано

Полностью упрощена система выбора моделей. Теперь она делает именно то, что нужно:

### 1. Загрузка топ-20 моделей ✅

- Подтягиваем реальный топ от LiveBench (или используем фолбэк данные)
- Убираем дубликаты моделей (разные версии одной модели усредняются)
- Сортируем по среднему скору

### 2. Четыре топа ✅

**🏆 ТОП-3 самых умных моделей**

- Сортировка по `coding_score` (качество кодинга)
- Пример: `qwen/qwen-2.5-coder-32b-instruct` (82.1%), `anthropic/claude-sonnet-4` (81.8%)

**💰 ТОП-3 самых дешёвых моделей**

- Сортировка по `price` (цена за токен)
- Пример: `deepseek/deepseek-chat`, `meta-llama/llama-3.1-70b-instruct` (~$0.2 per 1M tokens)

**⚡ ТОП-3 самых быстрых моделей**

- Сортировка по `speed` (токенов в секунду)
- Пример: `google/gemini-flash` (200 tok/s), `anthropic/claude-haiku-4` (150 tok/s)

**🎯 ТОП-3 Best Overall**

- Баланс всех трёх метрик: `40% качество + 30% цена + 30% скорость`
- Нормализация в 0-1 диапазон
- Пример: `google/gemini-2.0-flash-exp:free` (0.925), `google/gemini-flash-1.5` (0.879)

### 3. Тестирование моделей ✅

- Берём ТОП-3 из каждого топа (убираем дубликаты)
- Прогоняем по 3 тестовым задачам
- Оцениваем стоимость выполнения
- Визуализируем результаты

## Файлы

### `livebench_data.py`

Основной модуль с функциями:

- `fetch_top_models(top_n)` - загрузка топа моделей
- `enrich_models_with_metrics(models)` - добавление цены и скорости
- `get_top_by_quality(models, top_n)` - топ по качеству
- `get_top_by_price(models, top_n)` - топ по цене
- `get_top_by_speed(models, top_n)` - топ по скорости
- `get_top_overall(models, top_n)` - best overall баланс

### `01_model_comparison.ipynb`

Jupyter ноутбук с полным анализом:

1. Загрузка топ-20 моделей
2. Формирование 4 топов
3. Отбор уникальных моделей для тестирования
4. Оценка стоимости на тестовых задачах
5. Визуализация результатов

## Использование

```python
from livebench_data import (
    fetch_top_models,
    enrich_models_with_metrics,
    get_top_by_quality,
    get_top_by_price,
    get_top_by_speed,
    get_top_overall
)

# Загружаем топ-20 моделей
models = fetch_top_models(top_n=20)
models = enrich_models_with_metrics(models)

# Получаем топы
top_quality = get_top_by_quality(models, 3)  # Самые умные
top_cheap = get_top_by_price(models, 3)      # Самые дешёвые
top_fast = get_top_by_speed(models, 3)       # Самые быстрые
top_overall = get_top_overall(models, 3)     # Best overall

# Используем в своём коде
print(f"Лучшая модель: {top_quality[0]['model']}")
print(f"Качество: {top_quality[0]['coding_score']:.1f}%")
print(f"Цена: ${top_quality[0]['price']:.2e}/токен")
print(f"Скорость: {top_quality[0]['speed']:.0f} токенов/сек")
```

## Метрики

### Качество (`coding_score`)

- Процент правильных решений задач по кодингу
- Источник: LiveBench benchmark (ICLR 2025)
- Усреднение по категориям: coding, agentic_coding, mathematics

### Цена (`price`)

- Средняя стоимость за токен (input + output) / 2
- Источник: OpenRouter API (или эвристики)
- Единица: USD per token

### Скорость (`speed`)

- Скорость генерации токенов
- Оценка на основе размера модели и архитектуры
- Единица: tokens per second

### Overall Score

```python
overall = (quality_norm * 0.4) + (price_norm * 0.3) + (speed_norm * 0.3)
```

Где все метрики нормализованы в диапазон 0-1.

## Преимущества нового подхода

1. ✅ **Простота** - минимум кода, понятная логика
2. ✅ **Гибкость** - легко менять веса метрик для overall score
3. ✅ **Прозрачность** - видно как считаются все метрики
4. ✅ **Уникальность** - автоматическое удаление дубликатов моделей
5. ✅ **Практичность** - сразу тестируем на реальных задачах

## Пример результатов

```
🏆 ТОП-3 САМЫХ УМНЫХ:
  1. qwen/qwen-2.5-coder-32b-instruct              | score: 82.1
  2. anthropic/claude-sonnet-4                     | score: 81.8
  3. anthropic/claude-sonnet-3.5                   | score: 80.8

💰 ТОП-3 САМЫХ ДЕШЁВЫХ:
  1. deepseek/deepseek-chat                        | price: $2.00e-07
  2. meta-llama/llama-3.1-70b-instruct             | price: $2.00e-07
  3. perplexity/llama-3.1-sonar-large              | price: $2.00e-07

⚡ ТОП-3 САМЫХ БЫСТРЫХ:
  1. google/gemini-2.0-flash-exp:free              | speed: 200 tok/s
  2. google/gemini-flash-1.5                       | speed: 200 tok/s
  3. anthropic/claude-haiku-4                      | speed: 150 tok/s

🎯 ТОП-3 BEST OVERALL:
  1. google/gemini-2.0-flash-exp:free              | overall: 0.925
  2. google/gemini-flash-1.5                       | overall: 0.879
  3. anthropic/claude-haiku-4                      | overall: 0.861
```

## Что дальше?

- Можно добавить реальные вызовы API для тестирования качества ответов
- Можно интегрировать с вашей системой выбора модели
- Можно настроить веса для overall score под ваши приоритеты
- Можно добавить больше метрик (context length, latency, etc.)
